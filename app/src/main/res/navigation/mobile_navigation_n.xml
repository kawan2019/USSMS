<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation_n"
    app:startDestination="@+id/nav_home">
    <fragment
        android:id="@+id/nav_home"
        android:name="com.example.ussms.Fragment.HomeFragmentN"
        android:label="@string/menu_home"
        tools:layout="@layout/f_home_n">
        <action
            android:id="@+id/action_nav_home_to_nav_friend"
            app:destination="@id/nav_friend" />
    </fragment>
    <fragment
        android:id="@+id/nav_friend"
        android:name="com.example.ussms.Fragment.Friends"
        android:label="@string/menu_friends"
        tools:layout="@layout/f_friends">

    </fragment>
</navigation>
